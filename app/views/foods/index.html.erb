<h1>Food List</h1>
<ul>
  <% @foods.each do |food| %>
    <li><%= "#{food.name} (Quantity: #{food.quantity})" %></li>
  <% end %>
</ul>
<%= link_to 'Add New Food', new_food_path %>

<%= link_to 'Add New Food', new_food_path, class: "btn" %>

<table>
  <thead>
    <tr>
      <th>Food</th>
      <th>Measurement Unit</th>
      <th>Unit Price</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.measurement_unit %></td>
        <td><%= number_to_currency(food.price, unit: '$') %></td>
        <td>
          <%= link_to 'Delete', foods_path(food), method: :delete, data: { confirm: 'Are you sure you want to delete this item?'}, class: "btn" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


